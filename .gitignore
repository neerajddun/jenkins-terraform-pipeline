cat > demo/terraform/.gitignore << 'EOF'
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
tfplan
*.pem
EOF

cat > .gitignore << 'EOF'
# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
tfplan

# PEM keys - never commit these!
*.pem

# Node
node_modules/
EOF